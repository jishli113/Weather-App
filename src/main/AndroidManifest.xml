<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.weatherapp">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:requestLegacyExternalStorage="true"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.WeatherApp"
        android:usesCleartextTraffic="true">
        <activity android:name=".WeatherInfoPreferences"></activity>
        <activity android:name=".DesignPreferences" />
        <activity android:name=".Settings" />
        <activity android:name=".WeatherScreen" />
        <activity android:name=".SavedCities" />
        <activity
            android:name=".SearchScreen"
            android:label="@string/title_activity_search_screen"
            android:theme="@style/Theme.WeatherApp.NoActionBar" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>